# ⚡ 快速升级参考

## 🚀 一键升级（最简单）

### 升级到 v1.1.0
```bash
cd /opt/eleven_study
./upgrade.sh
```

### 升级到 v1.2.0（最新版）
```bash
cd /opt/eleven_study
./upgrade_to_v1.2.0.sh
```

按 `y` 确认，等待完成！

---

## 📦 包含文件

### v1.1.0
- ✅ `upgrade_v1.1.0.sql` - SQL增量更新脚本
- ✅ `upgrade.sh` - 自动升级脚本（带备份和验证）

### v1.2.0（最新）
- ✅ `upgrade_v1.2.0.sql` - SQL增量更新脚本
- ✅ `upgrade_to_v1.2.0.sh` - 自动升级脚本（带备份和验证）
- ✅ `v1.2.0_更新说明.md` - 版本更新文档

### 文档
- ✅ `升级指南.md` - 详细升级文档
- ✅ `快速升级.md` - 本文档

---

## 🔍 手动升级（三步走）

### v1.1.0

#### 1️⃣ 备份
```bash
docker compose exec mysql mysqldump -uroot -pliteracy2024 literacy_db > backup.sql
```

#### 2️⃣ 升级
```bash
docker compose exec -T mysql mysql -uroot -pliteracy2024 literacy_db < upgrade_v1.1.0.sql
```

#### 3️⃣ 重启
```bash
docker compose restart backend
```

### v1.2.0

#### 1️⃣ 备份
```bash
docker compose exec mysql mysqldump -uroot -pliteracy2024 literacy_db > backup_v120.sql
```

#### 2️⃣ 升级
```bash
docker compose exec -T mysql mysql -uroot -pliteracy2024 literacy_db < upgrade_v1.2.0.sql
```

#### 3️⃣ 重启
```bash
docker compose restart backend
```

---

## ✅ 验证成功

### v1.1.0
```bash
# 查看新表
docker compose exec mysql mysql -uroot -pliteracy2024 literacy_db -e "SHOW TABLES LIKE '%travel%';"

# 查看版本
docker compose exec mysql mysql -uroot -pliteracy2024 literacy_db -e "SELECT * FROM db_version;"

# 测试API
curl http://localhost:5000/api/travel-plans
```

### v1.2.0
```bash
# 查看新表
docker compose exec mysql mysql -uroot -pliteracy2024 literacy_db -e "SHOW TABLES LIKE '%reward%';"

# 查看版本
docker compose exec mysql mysql -uroot -pliteracy2024 literacy_db -e "SELECT * FROM db_version WHERE version='v1.2.0';"

# 测试API
curl http://localhost:5000/api/reward-items
curl http://localhost:5000/api/star-redemptions
```

---

## 🆘 出错恢复

```bash
# 恢复备份（替换为实际的备份文件名）
docker compose exec -T mysql mysql -uroot -pliteracy2024 literacy_db < backup.sql

# 重启服务
docker compose restart
```

---

## 📱 新功能访问

### v1.1.0 功能
访问应用后，查看新菜单：
- **古诗词** → 古诗背诵（+5⭐）
- **生活记录** → 旅行计划（1元=1⭐）
- **生活记录** → 日常任务（自定义⭐）

### v1.2.0 新功能（最新）
- **生活记录** → 旅行足迹（可视化地图 🗺️）
- **我的奖励** → 成就徽章（查看学习成就 🏆）
- **我的奖励** → 星星兑换（用星星换奖励 🎁）

---

**升级时间：** ≈ 2-5 分钟  
**停机时间：** ≈ 10 秒（仅重启后端）  
**风险等级：** 🟢 低（有自动备份）

