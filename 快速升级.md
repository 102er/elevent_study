# ⚡ 快速升级参考

## 🚀 一键升级（最简单）

```bash
cd /opt/eleven_study
./upgrade.sh
```

按 `y` 确认，等待完成！

---

## 📦 包含文件

- ✅ `upgrade_v1.1.0.sql` - SQL增量更新脚本
- ✅ `upgrade.sh` - 自动升级脚本（带备份和验证）
- ✅ `升级指南.md` - 详细升级文档

---

## 🔍 手动升级（三步走）

### 1️⃣ 备份
```bash
docker compose exec mysql mysqldump -uroot -ppassword literacy_db > backup.sql
```

### 2️⃣ 升级
```bash
docker compose exec -T mysql mysql -uroot -ppassword literacy_db < upgrade_v1.1.0.sql
```

### 3️⃣ 重启
```bash
docker compose restart backend
```

---

## ✅ 验证成功

```bash
# 查看新表
docker compose exec mysql mysql -uroot -ppassword literacy_db -e "SHOW TABLES LIKE '%travel%';"

# 查看版本
docker compose exec mysql mysql -uroot -ppassword literacy_db -e "SELECT * FROM db_version;"

# 测试API
curl http://localhost:5000/api/travel-plans
```

---

## 🆘 出错恢复

```bash
# 恢复备份
docker compose exec -T mysql mysql -uroot -ppassword literacy_db < backup.sql

# 重启服务
docker compose restart
```

---

## 📱 新功能访问

访问应用后，查看新菜单：
- **古诗词** → 古诗背诵（+5⭐）
- **生活记录** → 旅行计划（1元=1⭐）
- **生活记录** → 日常任务（自定义⭐）

---

**升级时间：** ≈ 2-5 分钟  
**停机时间：** ≈ 10 秒（仅重启后端）  
**风险等级：** 🟢 低（有自动备份）

